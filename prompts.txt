[16.07.2026 14:32] Anikey: Хлебопашев Антон Александрович:
Промпт: Разработка ядра логики и алгоритмов системы оптимизации мобильных расходов (на базе существующей БД)
Роль: Ты — Senior Backend Architect и Data Engineer.
Задача: Спроектировать и описать логику работы системы анализа тарифов и генерации рекомендаций, используя существующую реляционную схему БД (PostgreSQL), описанную ниже.
Критическое ограничение: КАТЕГОРИЧЕСКИ ЗАПРЕЩЕНО использовать JSON/JSONB поля для хранения данных, метаданных или результатов расчетов. Все данные должны храниться в нормализованных таблицах (users_numbers, reports, parameters, parameter_values). Гибкость достигается через конфигурационные таблицы и флаги, а не через динамические структуры.

Контекст существующей БД:
У вас уже есть следующая схема:

users_numbers: Хранит абонентов (id, number, username, limit_numbr).
Примечание: limit_numbr может использоваться как базовый лимит, но логика должна быть гибкой.
reports: Хранит отчетные периоды (id, report_date, report_month, subscriber_id -> ссылка на users_numbers.id).
Уникальное ограничение: один отчет на абонента в месяц.
parameters: Справочник типов услуг (id, name, description, category).
Примеры: "Исходящие звонки", "SMS", "Интернет", "Абонентская плата".
parameter_values: Хранит фактические данные (id, report_id, parameter_id, volume (текст, т.к. может быть "16583.25 Мбайт"), no_discount, discount, with_discount).
Связь: Один отчет -> много параметров (услуг).
parameter_values_2: (Возможно, дубликат или специфичная таблица, но основная логика строится на parameter_values).
Входные данные:

CSV-счет (ежемесячно): Парсер должен извлекать абсолютно все столбцы и строки из CSV и маппить их в таблицы parameters (если параметр новый) и parameter_values.
Excel-справочник (один раз): Загружается при старте. Данные маппятся в users_numbers (обновляя username, limit_numbr и создавая связи). После загрузки файл не используется.
Цель: Система должна автоматически анализировать потребление, сравнивать с тарифами, генерировать рекомендации и предоставлять гибкий интерфейс для управления отображением данных, не меняя структуру БД и не используя JSON.

1. Логика обработки данных (ETL Engine)
Опиши алгоритм парсинга и валидации, который работает с существующей схемой:

Универсальный парсер CSV:
Алгоритм:
Чтение CSV построчно.
Определение контекста: поиск строки с номером телефона (поиск/создание записи в users_numbers по number).
Создание записи в reports для текущего месяца (report_date, report_month, subscriber_id).
Извлечение услуг: Для каждой строки с услугой:
Извлечь название услуги (например, "Исходящие вызовы").
Маппинг в parameters: Проверить наличие услуги в таблице parameters по name и category.
Если нет: Создать новую запись в parameters (автоматическое добавление новых типов услуг из CSV).
Извлечь объем (парсить строку "16583.25 Мбайт" -> сохранить в volume как текст, но также выделить число и единицу измерения в отдельные поля, если возможно, или хранить как текст и парсить на лету).
Извлечь суммы (no_discount, discount, with_discount).
Создать запись в parameter_values (report_id, parameter_id, volume, no_discount, discount, with_discount).
Гибкость столбцов:
Если в CSV появляется новый столбец (например, "Код услуги"), система должна:
Создать запись в таблице parameters с name = "Код услуги" и category = "metadata".
Сохранить значение в parameter_values для соответствующего report_id.
Важно: Никаких JSON-полей. Новые данные просто становятся новыми строками в parameters и parameter_values.
Валидация (Data Quality Gate):
Перед сохранением проверить: сумма строк == итог счета; номер телефона валиден; ссылки на тарифы/сотрудников существуют.
При ошибке: запись в таблицу import_errors (создать её, если нет) с указанием report_id, row_number и причины. Транзакция откатывается для ошибочной строки.
2. Алгоритм рекомендаций (Recommendation Engine)
Опиши логику расчета рекомендаций, которая выполняется на уровне SQL-запросов или хранимых процедур, используя только таблицы users_numbers, reports, parameters, parameter_values.

Входные данные:
[16.07.2026 14:32] Anikey: Агрегированные данные из parameter_values за последние N

Хлебопашев Антон Александрович:
месяцев (например, 6).
Справочник тарифов (создать таблицу tariffs и tariff_limits, если нет, или использовать users_numbers.limit_numbr как базовый ориентир, но лучше создать отдельную таблицу для гибкости).
Алгоритм (псевдокод логики):

Агрегация потребления (SQL Window Functions):
Создать CTE abonent_stats:
Сгруппировать parameter_values по report_id -> subscriber_id.
Для каждого subscriber_id и category (из parameters.category) рассчитать:
avg_volume: Среднее значение (парсить volume в число).
max_volume: Максимальное значение.
total_cost: Сумма with_discount.
overage_count: Количество месяцев, где volume > limit (если лимит известен).
Реализация: Использовать AVG() OVER(PARTITION BY subscriber_id), PERCENTILE_CONT(0.9).
Симуляция сценариев (Cross Join Logic):
Для каждого абонента выполнить перекрестное соединение с таблицей tariffs (предполагаемая таблица: id, name, base_price, limits_json -> НЕТ, лучше tariff_limits: tariff_id, parameter_category, limit_value, overage_rate).
Рассчитать projected_cost для каждого тарифа:
Base_Price + SUM( MAX(0, avg_volume - limit_value) * overage_rate ) по всем категориям.
Добавить штрафной коэффициент, если max_volume > limit_value * 1.2.
Принятие решения (Business Rules Engine):
Сценарий DOWNGRADE (Понизить):
Условие: projected_cost < current_cost * 0.85 И max_volume < current_limit * 0.7.
Сценарий UPGRADE (Повысить):
Условие: total_overage_cost > current_base_price * 0.15 И overage_count >= 2.
Сценарий STAY (Оставить):
Условие: Разница < 5%.
Результат записать в таблицу recommendations (создать: id, subscriber_id, report_id, recommended_tariff_id, savings_amount, reason_code).
3. Механизм гибкости (Configuration-Driven Flexibility)
Опиши, как обеспечить максимальную гибкость без изменения кода и использования JSON:

Гибкость столбцов (Column Visibility):
Создать таблицу system_columns_config (или использовать parameters с флагом is_visible).
Поля: parameter_id, display_name, is_visible_in_ui (boolean), is_required_for_calculation (boolean).
Логика: API отдает список параметров, где is_visible_in_ui = true. Фронтенд строит таблицы динамически. Если администратор меняет флаг, столбец скрывается/показывается без перепарсинга данных.
Гибкость дашбордов (Widget Configuration):
Создать таблицу dashboard_widgets_config.
Поля: widget_id, widget_type, is_enabled, filter_criteria (ссылка на category или subscriber_id).
Логика: API возвращает список включенных виджетов. Фронтенд рендерит их в указанном порядке.
Гибкость отчетов:
API должен принимать параметры фильтрации (например, category=SMS, exclude_roaming=true), которые применяются к SQL-запросу на лету.
4. Требования к API (Strict Contract)
Опиши контракты API, которые возвращают строго типизированные данные:

GET /api/v1/abonents/{id}/recommendations
Возвращает список объектов: id, current_tariff_name, recommended_tariff_name, savings_amount, reason_text (из таблицы reasons), calculation_date.
Запрещено: Возвращать поля meta, extra_data.
POST /api/v1/bills/upload
Принимает файл. Возвращает taskId.
Статус проверки через GET /api/v1/tasks/{taskId}.
GET /api/v1/config/columns
Возвращает список всех parameters с флагами is_visible_in_ui.
PUT /api/v1/config/columns/{parameter_id}
Обновляет флаг is_visible_in_ui.
GET /api/v1/reports/dynamic
Параметры: category_ids[], time_range.
Возвращает данные, отфильтрованные на основе parameter_values и parameters.
5. Валидация и Аудит (Audit & Validation)
Валидация:
Все вставки в parameter_values должны проходить через проверку CHECK ограничений (например, volume не пустой).
При ошибке данные не сохраняются, а записываются в import_errors.
Аудит:
Любое изменение (смена тарифа, ручное исправление) должно записываться в таблицу audit_logs (создать: id, user_id, action, table_name, record_id, old_value, new_value, timestamp).
Важно: old_value и new_value должны храниться в нормализованном виде (например, в таблице audit_log_changes с полями column_name, old_val, new_val), а не в JSON.
[16.07.2026 14:32] Anikey: Хлебопашев Антон Александрович:
Промпт: Разработка системы аналитики и оптимизации мобильных расходов (Mobile Cost Intelligence)
Роль: Ты — Senior Python Backend Developer и Frontend Architect.
Задача: Разработать полноценное веб-приложение для анализа счетов мобильных операторов (формат CSV АО «Апатит»), генерации рекомендаций по тарифам и создания гибкого, настраиваемого дашборда.

Контекст и Входные данные:

CSV-файл (Счет Апатит_месяц.csv):
Сложная структура: разделитель ;, пустые строки, иерархия блоков по абонентам.
Содержит: номер, тариф, услуги (звонки, SMS, интернет), объемы (текст: "150 мин", "2.5 Гб"), суммы, НДС.
Excel-справочник (загружается один раз):
Связывает номера с ФИО, должностями, подразделениями.
После загрузки маппинг сохраняется в БД и больше не используется.
Существующая БД (PostgreSQL):
users_numbers: id, number, username, limit_numbr (лимит в рублях).
reports: id, report_date, report_month, subscriber_id.
parameters: id, name, description, category (voice, internet, sms).
parameter_values: id, report_id, parameter_id, volume (текст), no_discount, discount, with_discount.
Важно: Использовать только эти таблицы. Не создавать JSON-поля. Новые типы услуг добавляются как новые строки в parameters.
1. Backend: Логика и Алгоритмы
Разработать модульную систему на Python (FastAPI), разделенную на слои:

А. Модуль диагностики (diagnostics.py)
Создать скрипт/эндпоинт, который проверяет целостность данных перед расчетами:

Проверка связи: Подключение к БД, проверка доступности таблиц.
Проверка JOIN'ов:
Считать количество строк в parameter_values и reports.
Проверить, сколько записей parameter_values успешно связываются с reports (по report_id).
Проверить, сколько reports связываются с users_numbers (по subscriber_id).
Вывод: Если связь 0% — вывести критическую ошибку и примеры ключей для отладки.
Анализ категорий: Вывести топ-20 уникальных значений category и name из parameters для настройки классификатора.
Б. Модуль парсинга и ETL (parser.py)
Алгоритм парсинга CSV:
Чтение файла построчно, игнорирование пустых строк.
Идентификация абонента: Поиск строки с номером телефона. Создать/обновить запись в users_numbers.
Идентификация отчета: Создать запись в reports (месяц, дата).
Обработка услуг:
Для каждой строки с услугой:
Парсить название услуги. Проверить наличие в parameters. Если нет — создать новую запись (автоматическое добавление новых типов услуг).
Парсить объем: извлечь число и единицу измерения из строки (например, "16583.25 Мбайт" -> 16583.25, мбайт).
Сохранить в parameter_values (report_id, parameter_id, volume, with_discount).
Валидация:
Проверка сумм: сумма строк == итог счета.
Проверка ссылок: все subscriber_id существуют.
Ошибки записывать в лог, но не прерывать весь процесс.
В. Модуль бизнес-логики (domain.py)
Реализовать чистые функции (без SQL и HTTP), которые превращают сырые данные в отчеты:

Парсинг объемов (parse_volume):
Регулярное выражение для извлечения числа и единицы (мин, мбайт, гб, шт).
Нормализация единиц: мб -> мбайт, гб -> гб.
Классификация (categorize):
Логика: Проверка ключевых слов в названии услуги, категории и единице измерения.
Приоритет: Интернет (слова "интернет", "трафик", "гб") > SMS > Голос.
Возвращает: voice, internet, sms, other.
Расчет потребления (_category_usage):
Суммирование объемов и сумм по категориям для каждого абонента.
Конвертация Мбайт в ГБ.
Фильтрация исходящих звонков/SMS (если нужно).
Оценка риска (_status_and_risk):
Сравнивать total_cost с limit_numbr.
Статусы: normal (<100%), warning (100-125%), danger (>125%).
Риск: 0-100.
Генерация рекомендаций (build_recommendation):
Повысить: Если расход > лимита.
Понизить: Если расход < 60% лимита.
Оставить: Если в пределах.
Сuggestion: Рассчитать новый лимит с запасом 10% (suggest_limit).
Сборка отчетов:
build_month_view: Список абонентов с итогами, переплатой, статусом.
[16.07.2026 14:32] Anikey: build_number_detail: Детальная карточка с историей (по месяцам), трендом и списком услуг.
build_summary: KPI по компании (общий расход, экономия, топ-риски).
Г. API (FastAPI)
POST /upload/csv: Загрузка счета, запуск асинхронной задачи парсинга.

Хлебопашев Антон Александрович:
GET /api/recommendations: Список рекомендаций с фильтрами (по подразделению, статусу).
GET /api/abonents/{id}/detail: Детальная карточка с историей.
GET /api/config/columns: Список всех доступных столбцов (из parameters) с флагами видимости.
PUT /api/config/columns/{id}: Вкл/выкл отображение столбца.
GET /api/dashboard/widgets: Список включенных виджетов.
PUT /api/dashboard/widgets: Обновление списка виджетов.
2. Frontend: Гибкий и Адаптивный Интерфейс
Разработать SPA (React/Vue) с архитектурой Dashboard-as-a-Code:

Конструктор дашборда:
Страница должна состоять из сетки виджетов (Grid Layout).
Управление виджетами: Пользователь может включать/выключать любой виджет (KPI-карточки, графики, таблицы) через панель настроек.
Drag & Drop: Возможность менять порядок и расположение виджетов.
Создание виджетов: Кнопка "Добавить виджет" -> выбор типа (столбчатая диаграмма, линейный график, таблица) -> выбор данных (столбца из таблицы) -> сохранение конфигурации в БД.
Динамические таблицы:
Таблицы отчетов должны генерироваться динамически на основе конфигурации config/columns.
Чек-бокс "Показать расширенные данные" -> отображает скрытые столбцы (например, "Код услуги", "НДС").
Адаптивность:
Полная поддержка мобильных устройств (виджеты перестраиваются в одну колонку).
Современный дизайн (Material UI или Ant Design), приятные анимации.
Интерактивность:
Клик по элементу графика фильтрует остальные виджеты.
Возможность "Что если" (What-If): слайдер "Что если повысить тариф всем на 10%?" -> пересчет KPI в реальном времени.
3. Требования к данным и гибкости
Гибкость столбцов: Все столбцы CSV должны быть доступны для отображения. Новые столбцы, встречающиеся в CSV, автоматически добавляются в конфигурацию как скрытые по умолчанию.
Аудит: Любое изменение настроек (вкл/выкл виджетов, изменение видимости столбцов) должно логироваться.
Без JSON: Все данные хранятся в реляционных таблицах. Конфигурация интерфейса хранится в таблице dashboard_config (JSONB запрещен для данных, только для конфигурации, если это критично, но лучше использовать нормализованные таблицы widget_configs).
4. Ожидаемый результат
Предоставь:

Код Python:
diagnostics.py (скрипт проверки БД).
parser.py (логика парсинга CSV с маппингом в parameters и parameter_values).
domain.py (чистые функции расчетов: parse_volume, categorize, build_recommendation).
main.py (FastAPI приложение с эндпоинтами).
SQL-скрипты:
Миграции для создания таблиц конфигурации (dashboard_widgets_config, column_visibility_config), если их нет.
Примеры SQL-запросов для агрегации данных с использованием оконных функций.
Пример Frontend-кода:
Компонент DashboardGrid (React/Vue) с Drag & Drop.
Компонент DynamicTable, который рендерит столбцы на основе API.
Настройки виджетов (модальное окно создания/редактирования).
Инструкция:
Как запустить парсер.
Как добавить новый виджет.
Как настроить видимость столбцов.
Ключевое требование: Система должна быть максимально гибкой. Пользователь должен иметь возможность настроить вид под себя без программиста. Все данные из CSV должны быть сохранены и доступны для анализа. Никаких жестко зашитых полей в коде, кроме базовых (номер, сумма, дата).